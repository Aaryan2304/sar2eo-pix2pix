name: sar2eo
channels:
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
    - torch>=2.0
    - torchvision>=0.15
    - lpips>=0.1.4
    - scipy>=1.10
    - matplotlib>=3.7
    - tqdm>=4.65
    - pyyaml>=6.0
    - Pillow>=9.5
    - kaggle>=1.6
